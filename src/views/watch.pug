extends base

block content
    video(src=`/${video.videoUrl}`, controls)
    div
        button#play Play
        button#mute Mute
        span#time 00:00/00:00
        input(type="range", name="range", min="0", max="1", step="0.1")#volume

block scripts
    script(src="/build/js/videoPlayer.js") 