extends base
include mixins/video

block content
    div.profile__data
        if loggedInUser.avatarUrl === ""
            span 😄
        else 
            img.profile__avatar(src=loggedInUser.avatarUrl, crossorigin)
        h3=user.name
    div.video-grid
        each video in user.videos
            +video(video)
        else 
            span.empty__message No video found